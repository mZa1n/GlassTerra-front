# Base URL of the REST backend, without a trailing slash.
# Leave unset (or empty) to run against the bundled in-memory mock.
VITE_API_URL=

# Artificial latency of the mock transport, in milliseconds.
VITE_MOCK_LATENCY=250
